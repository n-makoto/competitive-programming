#include <bits/stdc++.h>
using namespace std;
#define rep(i, n) for (int i = 0; i < (int)(n); i++)
typedef long long ll;
 
int main() {
  int N;
  cin >> N;

  int A, B;
  cin >> A >> B;

  vector<int> vec(N);
  rep(i, N) {
    cin >> vec.at(i);
  }

  int ans = 0;
  cout << ans <<endl;
}
